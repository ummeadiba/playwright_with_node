import { ENV } from "@config/env";
import { test, expect, Locator } from '@playwright/test';

test.describe("Sample-Test-Script", () => {

    // Sample-Test-Case
    test('Sample-Test-Case', async ({ page }) => {
        // 
    });
});